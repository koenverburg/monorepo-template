{
  "name": "monorepo-template",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "packages:test": "pnpm test --filter '@packages/*'",
    "packages:build": "pnpm build --filter '@packages/*'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "esbuild": "^0.13.12",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.3.1"
  },
  "devDependencies": {
    "jest-esbuild": "^0.2.0",
    "typescript": "^4.4.2"
  }
}
