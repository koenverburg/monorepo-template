{
  "name": "monorepo-template",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "packages:test": "pnpm run test --filter @packages/*",
    "packages:build": "pnpm run build --filter @packages/*",
    "packages:clean": "pnpm run clean --filter @packages/*"
  },
  "author": "Koen Verburg <creativekoen@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "typescript": "^4.4.2"
  },
  "devDependencies": {
    "esbuild": "^0.12.24",
    "esbuild-jest": "^0.5.0",
    "jest": "^27.1.0",
    "rimraf": "^3.0.2"
  }
}
